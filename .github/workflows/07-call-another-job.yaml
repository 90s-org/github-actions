name: call workflows
on: [push] # Triggers the workflow on every push to the repository
jobs:
  # Job name
  feature-pipeline:
    if: github.ref_name == 'main'
    uses: 90s-org/workflows/.github/workflows/nodejs-eks-feature.yaml@main
    with:
      project: 'roboshop'
      component: 'catalogue'
  